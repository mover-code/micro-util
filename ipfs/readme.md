# IPFS upload golang

## ipAddr ipfs

```golang
    Init("addr").Sh.Add(strings.NewReader("hello world"))
```

## NftStorage ipfs

```golang
    AddStorage(strings.NewReader("today is rain"), "token")
```
